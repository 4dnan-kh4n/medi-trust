import { useState } from 'react'
import { Link, useNavigate } from 'react-router-dom'
import axios from 'axios'
import Logo from '../components/common/Logo'
const api = axios.create({ baseURL: import.meta.env.VITE_API_URL || 'http://localhost:5000/api', withCredentials: true })
export default function AdminLogin() { const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [status, setStatus] = useState(''); const navigate=useNavigate(); async function submit(event) { event.preventDefault(); setStatus(''); try { await api.post('/auth/login', { email, password }); navigate('/admin') } catch (error) { setStatus(error.response?.data?.error?.message || 'Sign-in failed. Try again.') } } return <main className="grid min-h-screen place-items-center bg-canvas px-5"><form onSubmit={submit} className="w-full max-w-md rounded-3xl border border-line bg-white p-8 shadow-[0_18px_50px_rgba(9,35,74,0.08)]"><Logo /><p className="mt-10 text-xs font-bold uppercase tracking-[.14em] text-forest">Admin access</p><h1 className="mt-3 font-display text-4xl text-ink">Sign in securely.</h1><label className="mt-7 grid gap-2 text-sm font-bold text-ink">Email<input required type="email" value={email} onChange={(event) => setEmail(event.target.value)} className="rounded-xl border border-line px-3 py-3 font-normal outline-none focus:border-forest" /></label><label className="mt-4 grid gap-2 text-sm font-bold text-ink">Password<input required type="password" value={password} onChange={(event) => setPassword(event.target.value)} className="rounded-xl border border-line px-3 py-3 font-normal outline-none focus:border-forest" /></label><button className="mt-6 w-full rounded-full bg-ink px-5 py-3 text-sm font-bold text-white transition hover:bg-forest">Sign in</button>{status && <p className="mt-4 text-sm text-muted" role="status">{status}</p>}<Link to="/" className="mt-6 inline-block text-sm font-bold text-forest">Return home</Link></form></main> }
